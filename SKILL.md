---
name: k-realty-yield
description: 한국 부동산의 임대수익률·레버리지 수익률을 계산한다. 주거용·상업용(오피스텔·상가)과 일반매매·경매·분양을 모두 다루며, 「표면 5%」가 취득 부대비용·공실·보유운영비·재계약비·임대소득세를 거쳐 실제로 얼마가 남는지를 단계별 낙차로 분해한다. 대출을 끼면 자기자본수익률이 어떻게 변하는지, 금리가 어디서 부호를 뒤집는지를 민감도표로 낸다. 모든 제도 값에 층(L1 법률~L4 행정지도)과 확인일이 붙는다. 규정집의 값과 실제 적용값이 다를 때(LTV 등) 둘을 함께 보여 준다. Use when 부동산 수익률·임대수익률·표면수익률·실질수익률·투자수익률 계산, 월세 받으면 연 몇 %인지, 대출 끼고 사면 수익률이 어떻게 되는지, 오피스텔·상가 수익률, 경매로 사면 수익률이 나아지는지, 전월세 전환율 상한, 취득세·보유세를 넣은 실수익 계산을 물을 때. 개별 물건 추천·매수 시점·가격 예측은 하지 않는다.
---

# k-realty-yield — 부동산 수익률 계산

**이 스킬은 사용자가 준 숫자로 산술을 해 주는 계산기다.** 물건을 고르지 않고, 시점을 고르지 않으며, 미래를 예측하지 않는다. 경계는 `DISCLAIMER.md`가 정한다. 셋은 기능이 없어서 못 하고, **「단일 답을 주지 않는다」만은 네가 지켜야 한다**(§5).

---

## 0. 먼저 읽을 것

| 파일 | 언제 |
|:---|:---|
| `DISCLAIMER.md` | **답을 내기 전 항상** — 경계 넷과 면책 |
| `policy/schema.md` | 제도 값을 인용할 때 — 층(L1~L6) 규약 |
| `MAINTENANCE.md` | 값이 만료됐다는 경고가 뜰 때 |

---

## 1. 대화 흐름

### 1-0. ⚠⚠ 물건 유형을 **먼저** 묻는다

```python
from engine.market import load
m = load(property_type="오피스텔")   # 반드시 넘긴다
```

**「오피스」와 「오피스텔」은 다른 물건이다.** 한국부동산원 임대동향조사의 대상은
**오피스 · 중대형 상가 · 소규모 상가 · 집합 상가**뿐이고 **오피스텔·아파트는 조사 대상이 아니다**(2026. 8. 16. 실측).

유형을 안 넘기거나 조사 대상이 아니면 **공실률·소득수익률을 싣지 않고 경고한다.** 그때는 사용자에게 직접 물어야 한다 — **0으로 두면 안 된다.**

### 1-1. 아는 값만 묻는다

**필수는 셋뿐이다** — 취득가 · 보증금 · 월세. 나머지는 없으면 없는 대로 계산하고 **빠진 항목을 결과에 표시한다.**

```
필수    취득가 · 보증금 · 월세
있으면  취득세 · 중개보수 · 공실 개월 · 보유운영비 · 임대소득 한계세율
대출    대출액 · 금리   (없으면 무차입 계산)
```

⚠ **모르는 값을 지어내지 않는다.** 사용자가 취득세를 모르면 **0으로 계산하고 「취득세 미반영」을 결과에 적는다.** 추정치를 넣으면 그 값이 근거처럼 보인다.

### 1-2. 다섯 셈법을 **동시에** 낸다

```python
from engine.yield_basic import Property, five_measures, net_yield_steps
```

**첫 화면이 이것이어야 한다.** 「중개업소가 말한 5%」가 어느 칸인지 사용자가 알아야 하기 때문이다.

| 셈법 | 분자 | 분모 |
|:---|:---|:---|
| 표면수익률 | 연 월세 | 취득가 − 보증금 |
| **실질수익률** | 월세 − 공실 − 운영비 − 재계약비 − 세금 | 취득가 + 부대비용 − 보증금 |
| 투자수익률 ROI | 월세 − 대출이자 | 취득가 − 대출 − 보증금 |
| 순영업소득 NOI | (금액이다. 비율 아님) | — |
| 시세차익형 | 시세 − 원가 | 실투자금 |

### 1-3. 단계 분해를 보여 준다

`net_yield_steps()`가 표면 → 실질의 다섯 단계와 **각 단계의 낙차(%p)**를 준다.

⚠ **낙차가 요점이다.** 실측 사례에서 **공실 두 달의 낙차가 취득세·중개보수·등기비를 전부 합친 것보다 컸다.**

### 1-4. 대출이 있으면 **표로** 낸다

```python
from engine.leverage import rate_sensitivity, sign_flip_point
from engine.market import load, suggested_rate_band
```

⚠⚠ **금리를 하나로 찍지 않는다.** 금리 3.5%p 차이가 자기자본수익률을 28%p 움직인다. `suggested_rate_band()`로 구간을 만들어 **부호가 뒤집히는 지점**을 보인다.

```
자기자본수익률 = 자산수익률 + (자산배율 − 1) × (자산수익률 − 조달비용)
```

**부호 전환점**(자산수익률 = 조달비용)은 손익분기보다 **먼저** 온다 — 그 사이에서 **돈을 벌면서 레버리지 때문에 손해를 본다.**

⚠ 무이자 보증금이 섞이면 `weighted_funding_rate()`로 **가중평균**을 낸다. 안 그러면 계산이 틀린다.

### 1-5. ⚠⚠ 경고를 지우지 않는다

```python
from engine.policy_loader import load_all, collect_warnings
```

**결과에 반드시 함께 낸다.** 특히 이것.

> ⚠⚠ 규정값 50% ≠ 실제 적용값 40% — **조문만 보면 틀립니다.**

`L4`(행정지도) 값은 **예고 없이** 바뀐다. 만료되면 경고가 뜬다. 30일을 넘기면 **사용자 입력을 요구한다.**

---

## 2. 갈래를 먼저 가른다

**같은 오피스텔이 세목마다 다른 물건이 된다.** 주거용인지 업무용인지 묻지 않고 계산하면 안 된다.

**⚠⚠ 다만 취득세는 그 예가 아니다 — 오피스텔은 갈리지 않는다.**

| | 상업·업무용 오피스텔 | 주거용 오피스텔 |
|:---|:---|:---|
| **취득세** | **4.6%** | **4.6% — 같다** ⚠ |
| 주택 수 산입 | 안 됨 | **재산세가 주택으로 과세되면 가산** |
| 종부세 | 부속토지만 별도합산 | 주택분 과세 |
| 임대료 부가세 | **과세**(건물분 10%) | 면세 |
| 임대소득 | 사업소득 종합과세 | 2천만원 이하 분리과세 선택 |

### 2-1. ⚠⚠ 오피스텔 취득세를 「주택 세율」로 계산하지 않는다

**지방세법 제11조 제1항 제8호**는 주택 세율(1~3%)의 대상을 이렇게 한정한다(요지 — 괄호 안 단서 생략).

> 유상거래를 원인으로 주택[「주택법」 제2조제1호의 주택으로서 「건축법」에 따른 건축물대장·사용승인서·임시사용승인서나 「부동산등기법」에 따른 등기부에 주택으로 기재 … 된 주거용 건축물과 그 부속토지를 말한다]을 취득하는 경우에는 …

⚠⚠ **요건이 둘이고 「으로서」로 묶여 있다.** 둘 다 갖춰야 주택 세율을 받는다.

| | 요건 | 오피스텔 |
|:---:|:---|:---|
| ⓐ | 「주택법」 제2조제1호의 **주택**일 것 | ❌ **여기서부터 탈락** |
| ⓑ | 건축물대장·등기부에 **주택으로 기재**될 것 | ❌ 업무시설로 기재 |

**ⓐ를 앞세우는 것이 중요하다.** 주택법 제2조 제1호의 주택은 **「단독주택과 공동주택으로 구분한다」**고 닫혀 있다. 오피스텔은 그 둘이 아니라 같은 조 **제4호의 「준주택」**이다 — **주택법 시행령 제4조 제4호가 오피스텔을 준주택으로 명시한다.** ⓑ만 들면 **「대장을 주택으로 바꾸면 되지 않나」**는 오해를 부른다. ⓐ가 있어 그 길이 막혀 있다.

→ 제8호에 못 들어가므로 **제7호 나목(농지 외의 것) 1천분의 40 = 4.0%** 적용.

```
취득세 4.0% + 지방교육세 0.4% + 농특세 0.2% = 4.6%
```

**주거용으로 쓰든, 몇 채를 갖고 있든 같다.** 다주택 중과(제13조의2)도 걸리지 않는다.

⚠ **다만 「고정」은 용도·주택 수에 대해서다 — 취득 원인은 별개다.** 위 4.6%는 **유상승계취득**(제7호)의 값이다. 상속은 제1호 나목 2.8%, 무상취득(증여 등)은 제2호 3.5%, 원시취득은 제3호 2.8%로 **각각 다르다.**

⚠ **혼동하기 쉬운 자리** — **제105조에 따라 재산세를 주택분으로 과세하는** 오피스텔은 **제13조의3 제4호**로 **주택 수에 가산**된다. 기준은 **「주거용으로 쓰는지」가 아니라 「재산세가 주택으로 과세되는지」**다(신고로 전환되며 자동이 아니다). 그리고 그것은 **그 오피스텔 자체의 취득세**가 아니라 **나중에 다른 주택을 살 때** 그 주택의 취득세가 중과될 수 있다는 뜻이다. **두 개를 섞지 않는다.**

⚠ 조문 확인일: **2026. 8. 16.**(지방세법 시행 2026. 7. 1. · 주택법 시행 2026. 8. 4.). 그 이후의 개정·해석 변경은 반영돼 있지 않다. **신고·납부 전에 과세관청 또는 세무전문가에게 확인할 것.**

⚠ 아파트·다세대 등 **주택**을 계산할 때는 당연히 제8호의 주택 세율과 중과가 걸린다. 유형을 먼저 묻는 이유가 여기에도 있다(§1-0).

값과 조문은 `policy/constants.yaml`에 층과 함께 있다.

---

## 3. 취득 경로 — 경매면 분모가 달라진다

**분자(임대료)는 취득 경로를 모른다. 경매가 수익률을 올린다면 오직 분모를 통해서다.**

| 분모의 칸 | 일반 매매 | 경매·공매 |
|:---|:---|:---|
| 부대비용 — 중개 | 매매 중개보수 | **없다** |
| 부대비용 — 명도 | 없다 | **명도 기간 이자·이사비·집행비**(전부 `L6`) |
| 보증금 | 협의 | **권리분석이 정한다** |

⚠⚠ **낙찰가율을 이 식에 그대로 넣을 수 없다** — 감정가 대비인데 분모는 실제 지불액이다. 감정가와 시세의 오차는 **양방향**이다.

⚠ 권리분석은 이 스킬의 일이 아니다 → **`k-auction-review`**.

---

## 4. 시장값 (선택)

```python
from engine.market import load, funding_rate_default
```

수집물이 있으면 조달비용·공실률 기본값이 시장 실측으로 바뀐다. **없으면 `None`이고 사용자에게 되묻는다.**

```bash
python3 data/collect_ecos.py     # 금리 — 시험키로도 동작
python3 data/collect_reb.py      # 공실률·순영업소득 — 키 필수
```

### 4-1. ⚠⚠ 전월세 전환율 상한 — 「기준금리 + 2%p」로 답하지 않는다

```python
from data.collect_ecos import conversion_rate_cap
conversion_rate_cap(2.75, "주택")   # 4.75
conversion_rate_cap(2.75, "상가")   # 12.00  ← 7.25%p 다르다
```

조문은 **「다음 각 호 중 낮은 비율」**이다 — **상한이 둘이고 작은 쪽이 이긴다.**

| | 상한 | 기준금리 연동분 | 근거 |
|:---|:---:|:---|:---|
| 주택 | 연 **1할** | 기준금리 **+ 2%p** | 주임법 §7-2 · 시행령 §9 |
| 상가 | 연 **1할2푼** | 기준금리 **× 4.5배** | 상임법 §12 · 시행령 §5 |

⚠ **두 가지를 틀리기 쉽다.**
- 기준금리만 더하면 **금리가 높을 때 법정 상한을 넘긴 값**을 낸다(주택은 8% 초과 구간).
- 상가에 주택 식을 쓰면 **크게 빗나간다** — 상가는 더하기가 아니라 **곱하기**다.

⭐ 그래도 요점은 남는다 — **법도 시행령도 안 고치고 금융통화위원회가 값을 움직인다.**

---

## 5. 하지 않는 것 — 물으면 거절한다

| 요청 | 답 |
|:---|:---|
| 「어느 지역이 좋나」 | **물건을 고르지 않는다.** 사용자가 준 물건만 계산한다 |
| 「지금 사도 되나」 | **시점을 고르지 않는다** |
| 「3년 뒤 얼마 오르나」 | **예측하지 않는다.** 상승률은 사용자가 가정으로 명시해야 하고, 그 경우 「가정」으로 표시한다 |
| 「연 몇 %냐」 단답 | **단일 답을 주지 않는다** — 전제와 구간을 함께 낸다 |

**앞의 셋은 기능이 아예 없어서 못 한다** — 추천·순위·검색·매매신호 코드가 없다.

⚠⚠ **넷째만은 다르다.** 「단일 답을 주지 않는다」를 막는 코드는 없다. `net_yield()`는 값 하나를 돌려준다. **이 경계를 지키는 것은 너다** — 전제와 구간을 함께 내지 않으면 이 스킬의 경계가 깨진다.

---

## 6. 출력 형식

```
■ 다섯 셈법        — 같은 물건, 다른 셈법
■ 표면 → 실질      — 단계별 낙차(%p)
■ 금리 민감도      — 구간과 부호 전환점 (대출이 있으면)
■ 전제             — 사용자가 준 값과 **비운 값**
■ ⚠ 경고          — 층·확인일·규정값↔실제값 (지우지 않는다)
```

마지막에 **아래 한 줄을 그대로** 붙인다. 요약하거나 생략하지 않는다.

> 이 결과는 입력하신 전제에 대한 산술이며 **투자·세무 자문이 아닙니다.** 세액과 대출 조건은 과세관청·금융기관에 확인하십시오.

⚠ 세목 해석(§2-1 등)을 답에 실었다면 **확인일도 함께** 밝힌다 — 그 해석은 `policy/constants.yaml`의 `checked_at` 현재의 것이고 그 뒤의 개정을 반영하지 않는다.
