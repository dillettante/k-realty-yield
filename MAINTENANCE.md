# MAINTENANCE — 갱신 절차

버전 1.0 · 2026. 8. 16. · **월 1회 갱신**

---

## 0. 왜 이 문서가 계산 코드보다 중요한가

**계산식은 한 번 맞으면 안 틀린다. 값은 매달 틀린다.**

이 도구가 다루는 숫자는 세 종류인데 **바뀌는 속도가 열 배씩 차이 난다.**

| 층 | 무엇 | 누가 바꾸나 | 실측된 속도 |
|:---:|:---|:---|:---|
| `L1` 법률 | 취득세율·장기보유특별공제 | 국회 | **재건축초과이익 부과율은 2006년 제정 이래 한 번도 안 바뀌었다** |
| `L2` 시행령 | 공정시장가액비율·대주주 기준 | 정부 단독 | 대주주 금액 기준은 **11년에 5회** |
| `L3` 고시·감독규정 | 은행업감독규정 별표6·기본형건축비 | 기관 | 기본형건축비는 **부칙 43개** |
| **`L4` 행정지도** | **LTV 실제 적용값** | **발표 하나** | **예고가 없다** |

⚠⚠ **가장 위험한 것은 `L4`다.** **은행업감독규정 별표6은 지금도 70%(규제지역 50%)인데 실제 적용은 40%**이고, 별표6은 그 뒤 **두 차례 개정되면서도 이 숫자를 손대지 않았다.** 조문을 정확히 찾은 사람이 10%p 틀린 값을 **확신을 갖고** 얻는다.

**⇒ 이 도구의 최소 조건: 관리를 못 한 달에도 사용자를 다치게 하지 않는다.** 그래서 모든 값에 **만료일**을 박고, 만료가 지나면 **계산은 하되 경고를 붙인다.**

---

## 1. 값의 수명 규약

`policy/*.yaml`의 모든 항목은 아래 **일곱 필드**를 반드시 갖는다. 하나라도 없으면 **로더(`engine/policy_loader.py`)가 로드를 거부한다.**

⚠ **CI는 없다.** 검사 주체는 테스트를 돌리는 사람이다.

```bash
python3 tests/test_policy.py
```

```yaml
- id: T-대출-02-규제지역              # ← 필수
  name: "LTV 실제 적용값 (규제지역)"   # ← 필수
  layer: L4                          # ← 필수. L1~L6 — 갱신 주기를 이 값이 정한다
  basis: "은행업감독규정 별표6 제2호 가목 + 금융위 행정지도"   # ← 필수
  checked_at: 2026-08-16             # ← 필수. 사람이 눈으로 확인한 날
  expires_at: 2026-09-16             # ← 필수. 이 날이 지나면 경고
  status: 개정예정                    # ← 필수. 확인완료/시행일확인/확인필요/개정예정
  # ── 아래는 선택 ──
  regulation_value: 0.50             # ⚠ 규정집의 값 (은행업감독규정 별표6)
  effective_value: 0.40              # ⚠ 실제 적용값 (행정지도)
  source: "금융위원회 보도자료 2025-10-16"
  source_url: "https://..."
  effective_from: 2025-10-16         # 모르면 null — 추측해 채우지 않는다
  note: "규정값과 실제값이 10%p 다르다. 조문만 보면 틀린다."
```

⚠⚠ **`expires_at`이 필수인 이유** — 없으면 `age_state()`가 그 값을 **영원히 「fresh」로 본다.** 「값의 나이를 준다」는 이 repo의 주장이 통째로 선택 사항이 된다. 그래서 만료일을 모르면 「신선」이 아니라 **「만료」로 본다.**

⚠ **같은 id를 두 파일에 두지 않는다.** 로더가 거부한다 — 같은 사실이 두 번 세어지고 만료일이 따로 낡는다(실제로 `T-대출-02`의 만료일이 파일마다 하루 어긋나 있었다).

### 1-1. 층별 기본 만료 기간

| 층 | 만료 | 근거 |
|:---:|:---:|:---|
| `L1` 법률 | **12개월** | 국회를 거쳐야 바뀐다. 다만 §2-2의 계절 확인은 별도 |
| `L2` 시행령 | **6개월** | 정부 단독으로 바뀐다 |
| `L3` 고시 | **3개월** | 기관이 바꾼다 |
| **`L4` 행정지도** | **1개월** | **예고가 없다 — 월 1회 확인의 실제 대상** |
| `L5`·`L6` | — | ⚠ **값을 적지 않는다.** 「어디서 확인하는가」만 적는다 |

⚠ **`L5`(기관 요건)·`L6`(실무 관행)에 값을 넣지 않는 것이 규약이다.** 법령이 아니라 기관·업계가 정해 A등급으로 확인할 출처가 없다. 명도비용·중개보수 관행·은행별 가산금리가 여기 속한다 — **사용자 입력으로 받는다.**

### 1-2. 만료 시 동작

| 상태 | 동작 |
|:---|:---|
| 만료 전 | 정상 계산 |
| **만료 후 30일 이내** | 계산하되 **결과 상단에 경고** — 「이 값은 YYYY-MM-DD 확인분이고 만료됐습니다. 출처에서 재확인하세요」 |
| **만료 후 30일 초과** | 계산하되 **해당 값을 결과에서 강조 표시**하고, `L4` 항목은 **사용자 입력을 요구** |

**계산을 막지 않는 이유** — 막으면 사용자가 다른 (더 낡은) 계산기로 간다. **틀릴 수 있음을 보이면서 계산해 주는 쪽이 안전하다.**

---

## 2. 월 1회 체크리스트

**매월 첫 영업일**에 돈다. 소요 20~30분.

### 2-1. 필수 — `L4` 확인 (이번 달에 반드시)

- [ ] **금융위원회 보도자료** — 가계부채·주택담보대출 관련 신규 발표가 있었나
  - 확인처: 금융위 보도자료 목록 (최근 1개월)
  - 있으면 → `policy/ltv.yaml` 갱신 + `checked_at`·`expires_at`·`source_url` 교체
  - 없으면 → **`checked_at`만 오늘로 갱신**(확인했다는 사실 자체가 기록이다)
- [ ] **은행업감독규정 별표6** — 조문이 실제 값을 따라잡았나
  - 확인처: 국가법령정보센터
  - ⚠ **따라잡았으면 그 사실이 중요하다** — 「규정집의 값이 실제 값이 아니다」가 해소되는 순간이므로, 경고 문구도 함께 내린다

### 2-2. 계절 확인 — 개정은 랜덤이 아니다

⭐ **근거는 개정 이력이다** — 소득세법 제59조의3은 12년 반에 다섯 번 바뀌었는데 **다섯 중 넷이 1월 1일 시행**이었다. **개정에는 계절이 있다.**

| 시기 | 무엇 | 할 일 |
|:---|:---|:---|
| **7~8월** | 정부 **세법개정안** 발표 | 🔴 `L1`·`L2` 전 항목을 개정안과 대조. **확정 아님을 반드시 표시** — 발표를 확정으로 앞질러 적는 오류가 바로 이 자리에서 생긴다 |
| **12월** | 국회 의결 | 🔴 개정안 중 통과분 확정. `expires_at`을 1월로 당긴다 |
| **1월 1일** | 대부분 시행 | 🔴 **연중 최대 갱신.** `L1`·`L2` 전수 재확인 |
| 분기 말 | 한국부동산원 R-ONE 공표 | 🟡 벤치마크 기본값 갱신 |
| 금통위 후 | 기준금리 | 🟢 자동(ECOS) — **전월세 전환율 상한이 함께 움직인다** |

### 2-3. 자동 — 확인만 (수집기가 도는지)

- [ ] `collect_ecos.py` 정상 동작(금리·물가)
- [ ] `collect_reb.py` 정상 동작(공실률·순영업소득)
- [ ] API 키 만료 여부

### 2-4. 분기 — `L1`·`L2` 조문 대조

- [ ] `policy/constants.yaml`의 조문을 국가법령정보센터에서 대조
- [ ] **시행예정본 확인** — 이미 공포됐으나 아직 시행 전인 개정이 있나
  - ⚠ **시행일이 하루도 도래하지 않은 채 폐지된 세금**도 있다. 「공포 = 시행」이 아니다

---

## 3. 긴급 갱신 트리거

**월 주기를 기다리지 않는다.**

| 트리거 | 왜 |
|:---|:---|
| **부동산 대책 발표** | LTV·DSR·규제지역이 한 번에 움직인다 |
| **규제지역 지정·해제** | 세금·청약·대출이 **함께 매달려 있다** |
| **금통위 기준금리 변경** | 전월세 전환율 상한이 자동으로 바뀐다 |
| **세법 시행일 도래** | 특히 1월 1일 |
| 사용자 이슈 제보 | 값이 틀렸다는 리포트 |

---

## 4. 갱신 기록

**모든 갱신은 여기에 한 줄씩 남긴다.** 값만 바꾸고 기록을 안 남기면 다음 사람이 「이 값이 언제 왜 바뀌었나」를 못 찾는다.

| 날짜 | 항목 | 변경 | 근거 | 확인자 |
|:---|:---|:---|:---|:---|
| 2026-08-16 | (초기 구축) | — | — | — |

⚠ **「확인했으나 변경 없음」도 기록한다.** 그것이 `checked_at`의 근거이고, **아무도 안 본 값과 보고도 그대로인 값은 다르다.**

---

## 5. 이 문서가 실패하는 방식 (미리 적어 둔다)

**정직하게 — 이 절차는 지켜지지 않을 가능성이 있다.** 개인이 운영하는 무료 도구의 현실이다. 그래서 실패해도 안전하도록 설계했다.

| 실패 | 결과 | 완화 |
|:---|:---|:---|
| 월 1회를 건너뛴다 | `L4` 값이 만료됨 | ✅ **경고가 자동으로 붙는다**(§1-2) |
| 몇 달을 건너뛴다 | 값이 크게 낡음 | ✅ 30일 초과 시 **사용자 입력 요구**로 전환 |
| 완전히 방치된다 | 도구가 죽음 | ✅ README에 **마지막 갱신일 배지**를 노출. 사용자가 스스로 판단 |
| 갱신자가 값을 잘못 읽는다 | 틀린 값이 확신과 함께 배포됨 | ⚠ **완화책 없음** — `source_url` 필수화로 추적만 가능하게 |

⚠⚠ **마지막 줄이 이 도구의 잔여 위험이다.** **성실하게 갱신해도 이용자가 안전해지는 것은 아니다** — 갱신은 값이 낡는 것을 늦출 뿐, 이용자가 그 값을 어떻게 쓰는지는 다른 문제다. 그래서 `DISCLAIMER.md`가 필요하다.

---

## 변경 이력

- **v1.0 (2026. 8. 16.)** — 신설. 월 1회 갱신 절차. **핵심은 「만료일 + 만료 시 경고」**이고, 근거는 셋이다 — 규정값 ≠ 실제값(LTV), 개정에 계절이 있다는 것, 층마다 개정 속도가 다르다는 것.

---

*MAINTENANCE v1.0 — 2026. 8. 16.*
